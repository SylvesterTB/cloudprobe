# CloudProbe

### A platform where developers upload code, and the system runs automated tests against it, version by version, then reports bugs, regressions, and performance changes.

The goal of this project is to provide a testing tool that allows for easy testing of files and classes and will utilize tools like Docker and AWS cloud storage
